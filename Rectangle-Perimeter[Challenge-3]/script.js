function findPerimeter(width,height)
{
  return 2*(width + height);
}

console.log(findPerimeter(10,20));


/*
Need to understand this approach as well
const howManySeconds = hours => Number.isInteger(hours) ? hours*3600 : "error";
*/